[test]
coverage = true
coverageSkipTestFiles = true
coverageReporter = ["text"]
# Number form = aggregate gate (the object form is enforced per-file by Bun in
# CI, which trips on the hidden per-file statement metric for files with a few
# untestable lines). Aggregate sits at ~98%.
coverageThreshold = 0.9
